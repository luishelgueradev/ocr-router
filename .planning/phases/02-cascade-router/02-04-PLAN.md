---
phase: 02-cascade-router
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - lib/v1/worker.js
  - lib/v1/router.js
  - test/worker.test.js
  - test/cascade-integration.test.js
  - package.json
autonomous: true
requirements: [CASC-05, CASC-06, JOB-02, JOB-04]
must_haves:
  truths:
    - "A request with no model runs the cascade via runCascade; the page-aware envelope is unchanged and pages[0].confidence is populated by the winning engine"
    - "The job result carries the JOB-02 trace (engines_attempted, winning_engine, low_confidence) additively on the existing envelope"
    - "A client can select profile fast/balanced/quality; an unspecified request uses balanced (the default)"
    - "Forcing a specific engine/model bypasses the cascade (single attempt, confidence recorded, no escalation); forcing an engine lacking the required capability is rejected pre-enqueue with a typed 422"
    - "GET /v1/models additionally lists the named profiles (id/default/description/engines) — completing API-05 profiles discovery"
    - "All Phase-1 tests remain green; the one Phase-1 assertion of pages[0].confidence===null is updated intentionally"
  artifacts:
    - path: "lib/v1/worker.js"
      provides: "runJob branches forced-model→single bypass vs no-model→runCascade; attaches trace; populates pages[0].confidence"
      contains: "runCascade"
    - path: "lib/v1/router.js"
      provides: "profile validation (allowlist), forced-engine capability 422, profiles discovery in GET /v1/models"
      contains: "profile"
    - path: "test/cascade-integration.test.js"
      provides: "End-to-end (mocked providers): profile selection, forced bypass, capability 422, trace present, low_confidence path"
      min_lines: 50
    - path: "package.json"
      provides: "test script registers the 4 new Phase-2 test files"
      contains: "cascade-runner.test.js"
  key_links:
    - from: "lib/v1/worker.js"
      to: "lib/v1/cascade/runner.js"
      via: "runCascade call when no model is forced"
      pattern: "runCascade"
    - from: "lib/v1/router.js"
      to: "lib/v1/cascade/config.js"
      via: "profile allowlist + capability table for forced-engine validation"
      pattern: "cascade/config"
    - from: "lib/v1/worker.js"
      to: "job result envelope"
      via: "attaches trace and populates pages[0].confidence"
      pattern: "trace"
---

<objective>
Wire the cascade into the live `/v1` request path so the product's core value is user-observable: an image POST with no model automatically escalates through the chain and returns the best result plus a trace, a client can pick a profile or force an engine (capability-validated), and `GET /v1/models` advertises the profiles — all without breaking a single Phase-1 test.

Purpose: This is the integration slice that turns the internal cascade (Plans 01-03) into an end-to-end capability. It is the only place worker.js and router.js change, and it owns the package.json test-suite registration for the whole phase.
Output: cascade-integrated `worker.js` + `router.js`, an end-to-end integration suite, the updated Phase-1 confidence assertion, and a registered green `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-cascade-router/02-CONTEXT.md
@.planning/phases/02-cascade-router/02-RESEARCH.md
@.planning/phases/02-cascade-router/02-01-SUMMARY.md
@.planning/phases/02-cascade-router/02-03-SUMMARY.md

<interfaces>
<!-- FROM Plan 03 (runner) -->
runCascade({ base64, mimeType, profile, deadlineSignal, budgetMs }) → { result:{ text, overlay?, engineId, provider, confidence }, trace }.

<!-- FROM Plan 01 (config) -->
CONFIG.profiles{fast,balanced,quality}, CONFIG.defaultProfile='balanced', CONFIG.capabilities[engineId]={provider,modes,supports_structured}.

<!-- Existing worker envelope (lib/v1/worker.js, DO NOT rename keys — additive only) -->
result = { text, pages:[{page:1,text,engine,confidence}], engine, provider, mode, bytes_received }.
Phase-1 worker.test.js currently asserts pages[0].confidence === null — this one assertion is updated in Task 1.

<!-- Existing router (lib/v1/router.js) -->
POST /v1/ocr resolves model (findModel or resolveDefaultEngine), reject-before-enqueue typed 422 pattern, limiter.schedule(runJob). GET /v1/models projects {id,name,provider,modes_supported,default_mode}. Body may carry model + mode; ADD profile.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate the runner into the worker (forced bypass vs cascade) and populate the trace</name>
  <files>lib/v1/worker.js, test/worker.test.js</files>
  <read_first>
    - lib/v1/worker.js (runJob — the single integration point; keep the finalized/early-return guard, page-aware envelope join, and logging intact)
    - 02-RESEARCH.md §"Runner signature & worker integration (D-11)" and §"Pitfall 6 (don't break the envelope shape)"
    - 02-CONTEXT.md D-07 (forced ⇒ no cascade), D-10 (trace), D-11 (worker integration, unchanged envelope)
    - test/worker.test.js (the D-04 success-path test that asserts pages[0].confidence===null — update this ONE assertion intentionally)
    - lib/v1/cascade/runner.js (runCascade signature) — from Plan 03
  </read_first>
  <action>
    Change runJob so its params accept a `profile` and a `forced` boolean (router sets forced=true when the client supplied an explicit model). FORCED path (bypass, D-07): keep the existing single runOCR call; compute confidence on its text via computeConfidence and record it; wrap the single attempt in a one-element trace (outcome passed|low_confidence per threshold, but NO escalation — forced runs exactly that engine). NO-MODEL path: build a job-level AbortController (deadline = budgetMs from the resolved profile), call runCascade({ base64, mimeType, profile, deadlineSignal: controller.signal, budgetMs }), then assemble the UNCHANGED envelope: `pages[0] = { page:1, text: result.text, engine: result.engineId, confidence: result.confidence }`, top-level `text` mirrors pages[0].text, `engine`/`provider` from the winner, and attach `trace` additively. Set the job-level `low_confidence` from trace.low_confidence. Do NOT rename/remove any existing envelope key (Pitfall 6). Keep the finalized early-return guard and the pino logging (log per-job outcome only — never buffers/keys, OPS-05). In test/worker.test.js: update the single `assert.equal(page.confidence, null, ...)` — since that test drives runJob with an explicit fake model it now exercises the forced-bypass path, so assert the confidence is a number in [0,1] (the computed value), NOT null; leave every other assertion (pages shape, succeeded status, joined text) unchanged.
  </action>
  <acceptance_criteria>
    - `node --test test/worker.test.js` exits 0 with the updated confidence assertion (number in [0,1], not null).
    - worker.js calls runCascade on the no-model path: `grep -c "runCascade" lib/v1/worker.js` >= 1.
    - The envelope keys pages/text/engine/provider are still present (no rename): `grep -cE "pages:|text:|engine:|provider:" lib/v1/worker.js` >= 1 and no test asserting the old shape breaks.
    - trace is attached to the result on the cascade path (verified by Task 3 integration test).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/worker.test.js</automated>
  </verify>
  <done>runJob branches forced-bypass vs cascade, attaches the JOB-02 trace, populates pages[0].confidence from the winner, keeps the envelope additive, and the updated worker.test.js is green.</done>
</task>

<task type="auto">
  <name>Task 2: Router profile selection, forced-engine capability 422, and profiles discovery</name>
  <files>lib/v1/router.js</files>
  <read_first>
    - lib/v1/router.js (POST /v1/ocr resolve-model + reject-before-enqueue 422 pattern; GET /v1/models projection — both extended here)
    - 02-RESEARCH.md §"Forced-engine capability validation (D-07, CASC-06)" (allowlist resolve, pre-enqueue 422, capability table) and §"Open Questions #3" (profiles discovery projects id/default/description/engines only — NOT thresholds)
    - 02-CONTEXT.md D-06 (profiles, default balanced), D-07 (forced bypass), D-09 (missing key clean drop)
    - lib/v1/cascade/config.js (CONFIG.profiles + CONFIG.defaultProfile + CONFIG.capabilities) — from Plan 01
  </read_first>
  <action>
    In POST /v1/ocr: read `profile` from req.body. Resolve it via an allowlist — `Object.hasOwn(CONFIG.profiles, profile)` — defaulting to CONFIG.defaultProfile when absent; reject an unknown profile with the existing typed 422 pattern (field:'profile'). When the client supplies an explicit `model` (forced): keep the existing findModel + provider-key 422 guards, additionally validate the forced engine against CONFIG.capabilities (mode support already checked via resolveMode; capability-mismatch — e.g. a future structured-only requirement — rejected pre-enqueue with a typed 422, field:'model'); pass `forced:true` + the resolved model into runJob. When NO model: pass `forced:false` + the resolved `profile` into runJob (the worker/runner assembles the chain and drops missing-key tiers per CASC-07 — do NOT pre-resolve a single default engine on the cascade path). Never bare-index config with raw user input (Pitfall 5). In GET /v1/models: add a `profiles` array projecting only `{ id, default:Boolean, description, engines: CONFIG.profiles[id].chain }` for fast/balanced/quality — do NOT expose thresholds (REQUIREMENTS out-of-scope). Keep the existing `models` projection unchanged so the Phase-1 API-03 test stays green.
  </action>
  <acceptance_criteria>
    - Unknown profile rejected: a POST with profile:'__proto__' or 'bogus' returns 422 field:'profile' (Task 3 test).
    - Forced incapable engine rejected pre-enqueue with typed 422 (Task 3 test); the buffer is never enqueued.
    - GET /v1/models includes a profiles array with fast/balanced/quality and no threshold field: `grep -c "profiles" lib/v1/router.js` >= 1; Task 3 test asserts no `threshold` key leaks.
    - Existing Phase-1 v1-routes API-03 models test still passes (models projection unchanged).
    - router resolves profile via allowlist: `grep -cE "Object.hasOwn\\(.*profiles" lib/v1/router.js` >= 1.
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./lib/v1/router')" && grep -Eq "Object.hasOwn\([^)]*profiles" lib/v1/router.js && grep -q "profiles" lib/v1/router.js && echo OK</automated>
  </verify>
  <done>Router validates profile via allowlist (default balanced), rejects forced incapable engines pre-enqueue with a typed 422, routes the no-model path to the cascade, and advertises profiles (no thresholds) via GET /v1/models.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end integration suite + register the Phase-2 tests in npm test</name>
  <files>test/cascade-integration.test.js, package.json</files>
  <read_first>
    - test/v1-routes.test.js (the e2e app-boot + fetch pattern for /v1/ocr → poll /v1/jobs/:id, and the require.cache runOCR mock style)
    - test/worker.test.js and test/cascade-runner.test.js (mock-provider technique to script per-engine outcomes deterministically)
    - package.json (the explicit `test` script file list — append the 4 new Phase-2 files)
    - 02-RESEARCH.md §"PRIORITY 5 trace" (the exact trace fields the e2e asserts)
  </read_first>
  <action>
    Write test/cascade-integration.test.js as an end-to-end suite (mocked providers via require.cache, no keys/network) exercising the live router+worker+runner path: (a) no-model POST with a scripted clean tier-1 result → job succeeds, result.trace.winning_engine==='ocrspace-engine2', engines_attempted.length===1, low_confidence false, pages[0].confidence is a number. (b) no-model POST with garbage tier-1 → escalates, winning engine is an LLM tier, trace shows the low_confidence first attempt. (c) all-fail scripted → job succeeds (never lost) with result.low_confidence===true / trace.low_confidence true and best-so-far text. (d) profile:'fast' selects the short chain; unspecified defaults to balanced. (e) forced model (explicit model in body) → single-attempt trace, no escalation even when its confidence is low (bypass). (f) forced incapable/unknown-capability engine → 422 pre-enqueue; unknown profile → 422. (g) GET /v1/models returns a profiles array without a threshold key. Then update package.json's `test` script to append `test/cascade-heuristic.test.js test/provider-signal.test.js test/cascade-runner.test.js test/cascade-integration.test.js` to the existing `node --test ...` file list (keep verify-redaction and all Phase-1 files). Run the FULL suite and confirm all Phase-1 tests plus the new ones are green.
  </action>
  <acceptance_criteria>
    - `node --test test/cascade-integration.test.js` exits 0 covering scenarios (a)-(g).
    - `npm test` exits 0 with ALL Phase-1 tests still passing AND the 4 new Phase-2 files registered and green (no Phase-1 regression).
    - package.json test script lists all four new files: `grep -c "cascade-integration.test.js" package.json` returns 1 and `grep -c "cascade-runner.test.js" package.json` returns 1.
    - The integration suite asserts result.trace exists with winning_engine + low_confidence, and that forcing an engine yields a single-attempt trace (no escalation).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/cascade-integration.test.js && npm test</automated>
  </verify>
  <done>End-to-end cascade behavior (stop/escalate/all-fail/profile/forced-bypass/capability-422/profiles-discovery) is proven through the live router+worker path, and the full npm test suite — Phase 1 + all Phase 2 — is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /v1/ocr (profile/model body fields) | untrusted strings select profile / force an engine |
| job result → client (trace field) | trace must expose codes/timings/confidence only, never secrets |
| forced-engine request → provider spend | an incapable/expensive forced engine must be gated pre-enqueue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | profile/model key lookup in router | mitigate | Resolve profile + model via Object.hasOwn / findModel allowlist; reject unknown with typed 422; never bare-index a plain object (Pitfall 5, ASVS V5) |
| T-02-14 | Denial of Service | forced-235B / expensive-engine spam | mitigate | Forced path still bounded by the Phase-1 queue + single attempt; 235B only reachable via cascade quality chain; capability 422 rejects pre-enqueue (no buffer held) |
| T-02-15 | Information Disclosure | trace field leaking secrets/PII, or profiles discovery leaking internal thresholds | mitigate | Trace = mapErrorCode codes + timings + confidence only (OPS-05 redaction covers logs); GET /v1/models projects id/default/description/engines, never thresholds |
| T-02-16 | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (native + Phase-1 deps); package.json change is test-file registration only, no new dependency (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npm test` exits 0: all Phase-1 tests green + 4 new Phase-2 test files registered and green (no regression).
- End-to-end cascade proven through the live /v1 path: stop-at-tier-1, escalate, all-fail best+low_confidence, profile selection (default balanced), forced bypass, capability/profile 422, profiles discovery.
- Envelope stays additive (trace + populated confidence); no envelope key renamed/removed.
</verification>

<success_criteria>
- CASC-05: named profiles selectable, balanced default; profiles discoverable via GET /v1/models (completes API-05).
- CASC-06: forced engine bypasses the cascade; incapable forced engine rejected with a typed 422.
- JOB-02/JOB-04: every job result carries the trace and is bounded by one authoritative deadline threaded from the worker.
- Zero Phase-1 regression: the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/02-cascade-router/02-04-SUMMARY.md` when done.
In the SUMMARY, record the two DEFERRED ops checks (NON-blocking, for PENDING-ISSUES): (1) live-key heuristic threshold calibration (run real clean/garbage/near-empty images through ocr.space + one LLM tier; confirm P50 clean docs stop at tier-1, garbage escalates; tune weights/thresholds via env — no code change); (2) confirm current Ollama Cloud subscription tier + observed quota headroom against the budget/max-tier defaults. Neither blocks Phase-2 completion — code reacts to the 429 response, not a fixed number.
</output>
