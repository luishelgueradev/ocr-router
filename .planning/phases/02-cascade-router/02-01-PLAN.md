---
phase: 02-cascade-router
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/v1/env.js
  - lib/v1/cascade/config.js
  - lib/v1/cascade/heuristic.js
  - test/cascade-heuristic.test.js
  - test/fixtures/heuristic/fixtures.js
autonomous: true
requirements: [CASC-03, CASC-09]
must_haves:
  truths:
    - "computeConfidence(text,{overlay}) returns a number in [0,1] for every input"
    - "Empty/whitespace text scores 0 (hard gate) and garbage-ratio > 0.30 scores 0 (hard gate)"
    - "A clean scanned-doc string with overlay scores >= 0.90 (passes all profiles ⇒ stops at tier-1 per SC#1)"
    - "Thresholds and weights come from config (env-overridable); changing an env re-runs the suite green without editing test logic"
    - "Routing chain, profiles, capability table, and heuristic bounds are declarative data in one module (no code branches)"
  artifacts:
    - path: "lib/v1/cascade/config.js"
      provides: "Declarative chain, profiles (fast/balanced/quality, default balanced), engine capability table, heuristic thresholds/bounds/weights"
      contains: "defaultProfile"
    - path: "lib/v1/cascade/heuristic.js"
      provides: "Pure computeConfidence + passesThreshold + garbageRatio helpers"
      exports: ["computeConfidence", "passesThreshold"]
    - path: "lib/v1/env.js"
      provides: "floatFromEnv beside intFromEnv"
      exports: ["intFromEnv", "floatFromEnv"]
    - path: "test/cascade-heuristic.test.js"
      provides: "10-fixture calibration matrix + tunability proof"
      min_lines: 40
  key_links:
    - from: "lib/v1/cascade/heuristic.js"
      to: "lib/v1/cascade/config.js"
      via: "reads cfg.heuristic thresholds/weights + cfg.profiles[p].threshold"
      pattern: "require.*cascade/config"
---

<objective>
Build the highest-risk piece of the cascade first: the **pure, config-driven confidence heuristic** and its declarative configuration, proven by a table-driven 10-fixture unit suite that needs no images, no keys, and no network.

Purpose: The heuristic is the single hardest logic in Phase 2 (STATE risk-flag: false-good garbage detection). Making it pure over `(text, overlay)` lets us calibrate thresholds without touching logic and discharges the risk-flag with a labeled fixture sample. The declarative `config.js` is the CASC-09 backbone every later plan consumes.
Output: `lib/v1/cascade/config.js`, `lib/v1/cascade/heuristic.js`, `floatFromEnv` in `lib/v1/env.js`, and a green `test/cascade-heuristic.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cascade-router/02-CONTEXT.md
@.planning/phases/02-cascade-router/02-RESEARCH.md

<interfaces>
<!-- Existing helper the config mirrors (lib/v1/env.js) -->
intFromEnv(name, fallback) — throws on non-integer or n<=0; returns fallback when unset/empty.
floatFromEnv (NEW, this plan) — same shape; accepts positive finite floats (0.60 valid); throws on non-finite/negative.

<!-- Engine ids the chain reuses, from lib/models.js -->
'ocrspace-engine2' (provider: ocrspace), 'ollama-gemini-3-flash', 'ollama-gemma4-31b', 'ollama-qwen3-vl-235b' (provider: ollama).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add floatFromEnv and the declarative cascade config</name>
  <files>lib/v1/env.js, lib/v1/cascade/config.js</files>
  <read_first>
    - lib/v1/env.js (the intFromEnv pattern to mirror — same throw-on-invalid contract)
    - lib/models.js (engine ids + provider fields the chain/capability table reuse)
    - 02-RESEARCH.md §"1f. Config block" (the exact heuristic + profiles + defaultProfile object) and §"PRIORITY 4 → Bounds (CASC-08)" and §"Forced-engine capability validation (D-07)"
    - 02-CONTEXT.md D-01 (declarative config), D-06 (profiles), D-08 (bounds)
  </read_first>
  <action>
    Add `floatFromEnv(name, fallback)` to lib/v1/env.js mirroring intFromEnv: return fallback when unset/empty; throw `"${name} must be a positive finite number, got: ${raw}"` when the parsed Number is not finite or is <= 0; export it alongside intFromEnv.
    Create lib/v1/cascade/config.js as a JS module (per Claude's Discretion — allows inline rationale + env helpers) exporting a single CONFIG object with: (a) `heuristic` block — absMinChars via intFromEnv('HEURISTIC_MIN_CHARS',1), goodChars via intFromEnv('HEURISTIC_GOOD_CHARS',16), goodWords via intFromEnv('HEURISTIC_GOOD_WORDS',4), maxGarbageRatio via floatFromEnv('HEURISTIC_MAX_GARBAGE',0.30), and weights w:{printable:0.5,length:0.3,overlay:0.2}; (b) `profiles` — fast/balanced/quality each with `chain` (ordered engine ids per research 1f: fast=[ocrspace-engine2,ollama-gemini-3-flash], balanced adds ollama-gemma4-31b, quality adds ollama-qwen3-vl-235b), `threshold` via floatFromEnv('PROFILE_{NAME}_THRESHOLD', 0.50/0.60/0.70), `maxAttempts` (2/3/4), `budgetMs` (30000/60000/120000); (c) `defaultProfile:'balanced'`; (d) `capabilities` table keyed by engine id with `{ provider, modes, supports_structured:false }` (include supports_structured now so Phase 4 is additive — false for ocrspace-engine2); (e) `minSliceMs` and `perEngineMs` bounds for the runner deadline (per research §"Single authoritative deadline"). Add inline comments citing D-01/CASC-09 and the LOW-confidence-calibration caveat on threshold numbers. Do NOT hard-code any routing decision as an if/branch — it is all data.
  </action>
  <acceptance_criteria>
    - `node -e "const {floatFromEnv}=require('./lib/v1/env'); if(floatFromEnv('X',0.6)!==0.6) process.exit(1)"` exits 0.
    - `node -e "process.env.X='-1'; require('./lib/v1/env').floatFromEnv('X',0.6)"` exits non-zero (throws on negative).
    - `node -e "const c=require('./lib/v1/cascade/config'); const p=c.profiles||c.CONFIG?.profiles; const cfg=c.CONFIG||c; if(cfg.defaultProfile!=='balanced') process.exit(1); if(!Object.hasOwn(cfg.profiles,'fast')||!Object.hasOwn(cfg.profiles,'quality')) process.exit(1); if(cfg.profiles.quality.chain.length!==4) process.exit(1)"` exits 0.
    - config.js contains no `if`/`switch` that selects an engine or profile (routing is data). Verify: `grep -v '^\s*//' lib/v1/cascade/config.js | grep -cE '\bif\s*\(|\bswitch\s*\('` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const c=require('./lib/v1/cascade/config'); const cfg=c.CONFIG||c; process.exit(cfg.defaultProfile==='balanced' && cfg.profiles.quality.chain.length===4 && require('./lib/v1/env').floatFromEnv('NOPE',0.3)===0.3 ? 0:1)"</automated>
  </verify>
  <done>floatFromEnv exported and validates like intFromEnv; config.js exports the declarative chain/profiles/capabilities/heuristic-bounds with defaultProfile='balanced' and no engine-selecting branches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — write the 10-fixture heuristic calibration suite</name>
  <files>test/cascade-heuristic.test.js, test/fixtures/heuristic/fixtures.js</files>
  <behavior>
    - clean_paragraph (~300-char Spanish text): score >= 0.90; passes fast/balanced/quality.
    - clean_receipt ("TOTAL: $12.50\nGracias por su compra"): score 0.75-0.90; passes all three.
    - clean_with_overlay (same text + mock overlay {HasOverlay:true, wordCount:40}): score >= 0.95; passes all three.
    - mixed_partial (~50% real + "����▯▯ Ã¿Ø£", garbage ~0.35): score 0 (hard gate); fails all three.
    - mojibake ("Ã¿Ø£�□□ ▯▯▯ \x00\x07"): score 0; fails all.
    - control_chars (>30% C0 controls): score 0; fails all.
    - near_empty ("OK"): score 0.55-0.62; passes fast only.
    - single_char ("a"): score ~0.5; passes fast only.
    - empty (""): score 0; fails all.
    - whitespace_only ("   \n\t "): score 0; fails all.
    - Every fixture: computeConfidence(...) is a finite number within [0,1].
    - Tunability proof: overriding an env threshold (e.g. PROFILE_BALANCED_THRESHOLD) and re-requiring config flips a borderline fixture's pass/fail WITHOUT editing test logic.
  </behavior>
  <read_first>
    - 02-RESEARCH.md §"1g. Fixture set" (the exact fixture table + expected bands + pass matrix) and §"1c/1d/1e" (signal + formula + threshold definitions)
    - test/modes.test.js (existing node --test table-driven style to match)
    - test/worker.test.js (require.cache override style, if a config env override needs a fresh require)
  </read_first>
  <action>
    Put the fixtures in test/fixtures/heuristic/fixtures.js as an exported array of `{ name, text, overlay?, band:[lo,hi], passes:{fast,balanced,quality} }` rows drawn verbatim from research §1g. Write test/cascade-heuristic.test.js as a table-driven `node:test` suite importing computeConfidence + passesThreshold from lib/v1/cascade/heuristic (which does NOT exist yet — this is the RED step) and the config. Assert for each fixture: (1) score is a finite number in [0,1]; (2) score falls within its band; (3) passesThreshold(score, profile) matches the passes matrix for all three profiles. Add one tunability test that sets an env override, clears the require cache for config.js (delete require.cache[require.resolve('../lib/v1/cascade/config')]), re-requires, and asserts a borderline fixture flips — proving thresholds are config-driven (CASC-09). Do NOT create heuristic.js in this task; the suite MUST fail at require/import time or on the first assertion (RED). Commit as `test(02-01): add failing heuristic fixture suite`.
  </action>
  <acceptance_criteria>
    - `node --test test/cascade-heuristic.test.js` exits NON-zero (RED — heuristic.js absent or assertions unmet).
    - test/fixtures/heuristic/fixtures.js exports exactly the 10 named fixtures from research §1g.
    - The suite references passesThreshold for fast, balanced, AND quality (grep confirms all three profile names appear).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/cascade-heuristic.test.js; test $? -ne 0 && echo RED-OK</automated>
  </verify>
  <done>The 10-fixture suite + fixtures file exist and fail (RED) because heuristic.js is not yet implemented; suite covers all three profiles and a tunability override.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — implement the pure confidence heuristic</name>
  <files>lib/v1/cascade/heuristic.js</files>
  <behavior>
    - Makes every assertion in test/cascade-heuristic.test.js pass.
    - computeConfidence normalizes NFC + trims, counts code points via [...t] (not .length), applies hard gates (len<absMinChars ⇒ 0; garbageRatio>maxGarbageRatio ⇒ 0), then a weighted average over PRESENT signals (printable, length, and overlay only when overlay.HasOverlay), renormalizing weights when overlay is absent.
    - passesThreshold(conf, profile) reads cfg.profiles[profile].threshold via an allowlist (Object.hasOwn), never a bare index.
  </behavior>
  <read_first>
    - 02-RESEARCH.md §"1c. Concrete signal definitions", §"1d. Combined confidence formula", §"1e. Per-profile thresholds", §"Pitfall 4 (code-unit vs code-point)", §"Pitfall 5 (prototype pollution)"
    - lib/v1/cascade/config.js (the cfg.heuristic weights/bounds + cfg.profiles thresholds this reads) — created in Task 1
    - test/cascade-heuristic.test.js (the contract to satisfy) — created in Task 2
  </read_first>
  <action>
    Implement lib/v1/cascade/heuristic.js as a PURE module (no I/O, no network, no keys). Export: `garbageRatio(text)` (counts bad code points — C0 controls except \t\n\r, DEL, C1 controls, U+FFFD, U+FFFE/FFFF, lone surrogates — over max(len,1)); `computeConfidence(text,{overlay}={},cfg=require('./config').heuristic-view)` returning a number in [0,1] per research §1d (hard gates first, then weighted renormalized soft score with printable/length always present and overlay word-count only when overlay?.HasOverlay); `passesThreshold(conf, profile)` that resolves the threshold via `Object.hasOwn(CONFIG.profiles, profile)` allowlist and throws/returns-false for unknown profiles (prototype-pollution guard, Pitfall 5). Read every weight/threshold/bound from config — no literals in the branch logic. Overlay signal is `overlay.wordCount` (a count, NOT a confidence — D-03 amended). Run the suite; iterate until GREEN. Commit as `feat(02-01): implement pure confidence heuristic`.
  </action>
  <acceptance_criteria>
    - `node --test test/cascade-heuristic.test.js` exits 0 (all 10 fixtures + tunability pass).
    - `node -e "const {computeConfidence}=require('./lib/v1/cascade/heuristic'); const bad=computeConfidence('Ã¿Ø£� '); if(bad!==0) process.exit(1); const ok=computeConfidence('a'.repeat(300)); if(!(ok>=0.9&&ok<=1)) process.exit(1)"` exits 0.
    - `node -e "const h=require('./lib/v1/cascade/heuristic'); const r=h.passesThreshold(1,'__proto__'); if(r===true) process.exit(1)"` exits 0 (unknown profile is not truthy — no prototype lookup).
    - heuristic.js performs no require of axios/fetch/providers (pure): `grep -cE \"require\\(.*(ocr|ollama|axios|providers)\" lib/v1/cascade/heuristic.js` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/cascade-heuristic.test.js</automated>
  </verify>
  <done>heuristic.js is a pure module driving the 10-fixture suite green; hard gates return 0, clean docs score high, unknown profiles are rejected via allowlist, all thresholds sourced from config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-supplied `profile` string → config profile lookup | untrusted key indexes the profiles object in passesThreshold |
| provider text → heuristic | untrusted OCR/LLM output scored for garbage; must not crash on hostile code points |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | passesThreshold profile lookup | mitigate | Resolve threshold via `Object.hasOwn(CONFIG.profiles, profile)` allowlist; never bare-index a plain object with raw input (Pitfall 5) |
| T-02-02 | Denial of Service | computeConfidence on hostile text | mitigate | Pure, allocation-bounded code-point scan (`[...t]`); no regex backtracking, no dictionary/network — deterministic O(n) |
| T-02-03 | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan (Node built-ins + Phase-1 deps only); no install surface (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node --test test/cascade-heuristic.test.js` exits 0 (RED→GREEN completed).
- config.js is pure declarative data (no engine-selecting branches); heuristic.js is a pure module (no provider/network requires).
- Existing Phase-1 suite untouched by this plan (no Phase-1 file modified except env.js additive export).
</verification>

<success_criteria>
- Pure, config-driven `computeConfidence` + `passesThreshold` exist and pass the labeled 10-fixture calibration matrix (discharges STATE heuristic risk-flag at unit level).
- Thresholds/weights are env-overridable data (CASC-09); a threshold tweak re-runs green without editing logic.
- Clean-doc sanity: a ~300-char clean string scores >= 0.90 and passes even `quality` — the basis for SC#1 (tier-1 stop).
</success_criteria>

<output>
Create `.planning/phases/02-cascade-router/02-01-SUMMARY.md` when done.
Note in the SUMMARY: threshold numbers are provisional (LOW-confidence pending live-key calibration — a deferred ops task, not a code blocker).
</output>
