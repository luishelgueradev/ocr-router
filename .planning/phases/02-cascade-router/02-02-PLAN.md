---
phase: 02-cascade-router
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/providers/ocrspace.js
  - lib/providers/ollama.js
  - lib/ocr.js
  - test/provider-signal.test.js
autonomous: true
requirements: [JOB-04, CASC-03]
must_haves:
  truths:
    - "runOCR accepts and forwards an AbortSignal to both providers; aborting the signal cancels the in-flight HTTP call (no wedged socket)"
    - "ocr.space requests enable overlay and return a scalar overlay.wordCount + overlay.HasOverlay the heuristic can consume"
    - "ocr.space returns OCRExitCode so the runner can treat 'all pages failed' as a hard failure even when ok:true"
    - "An aborted ocr.space fetch and an aborted ollama axios call both surface as ok:false (fall-through), never as a thrown crash"
  artifacts:
    - path: "lib/providers/ocrspace.js"
      provides: "Overlay-enabled ocr.space call returning {text, overlay:{HasOverlay,wordCount}, ocrExitCode}; accepts external signal"
      contains: "isOverlayRequired"
    - path: "lib/providers/ollama.js"
      provides: "Ollama call that accepts and forwards an AbortSignal via axios {signal}"
      contains: "signal"
    - path: "lib/ocr.js"
      provides: "runOCR threading opts.signal into both providers"
      exports: ["runOCR"]
    - path: "test/provider-signal.test.js"
      provides: "Abort + overlay-shape unit tests (mocked fetch/axios, no keys/network)"
      min_lines: 40
  key_links:
    - from: "lib/ocr.js"
      to: "lib/providers/ocrspace.js and lib/providers/ollama.js"
      via: "passes opts.signal through runOCR"
      pattern: "signal"
    - from: "lib/providers/ocrspace.js"
      to: "heuristic Signal C"
      via: "overlay.wordCount return field"
      pattern: "wordCount"
---

<objective>
Prepare both providers for cascade orchestration: thread a single job-level `AbortSignal` into ocr.space's `fetch` and ollama's `axios`, and enable ocr.space overlay to return the scalar word-count signal (Signal C) the heuristic consumes — while returning `OCRExitCode` so the runner can detect a hard failure hidden behind `ok:true`.

Purpose: JOB-04 requires one authoritative deadline that can actually abort a hung provider (Pitfall 3 — ollama's 5-min axios timeout must not outlive the job budget). CASC-03's overlay word-count signal requires enabling `isOverlayRequired` and extracting a count (NOT a confidence — D-03 amended). These are the provider-layer prerequisites; the runner (Plan 03) composes the signals.
Output: signal-aware `ocrspace.js`, `ollama.js`, `ocr.js`, and `test/provider-signal.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cascade-router/02-CONTEXT.md
@.planning/phases/02-cascade-router/02-RESEARCH.md

<interfaces>
<!-- Current provider signatures (to extend, not break) -->
runOCR(model, base64Image, mimeType, apiKey, opts) — dispatches on model.provider.
runOcrSpace(engine, language, base64Image, mimeType, apiKey) — returns { ok, timeMs, text } | { ok:false, status?, error }.
runOllama(modelTag, base64Image, apiKey, opts) — opts may carry { options, prompt }; returns { ok, timeMs, text } | { ok:false, status?, error }.

<!-- ocr.space overlay wire shape (RESEARCH §P2): ParsedResults[i].TextOverlay.Lines[].Words[] = {WordText,Left,Top,Height,Width}; HasOverlay:bool; top-level OCRExitCode (1 ok / 2 partial / 3 all-failed / 4 fatal) -->
<!-- axios honors { signal }; on abort throws err.code 'ERR_CANCELED'. fetch throws AbortError on signal abort. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable ocr.space overlay word-count + OCRExitCode, and accept an external signal</name>
  <files>lib/providers/ocrspace.js</files>
  <read_first>
    - lib/providers/ocrspace.js (current call: hardcoded isOverlayRequired='false' and signal: AbortSignal.timeout(2*60*1000) — both change here)
    - 02-RESEARCH.md §"PRIORITY 2 — ocr.space Overlay Extraction" (exact extraction snippet + OCRExitCode handling) and §"1a. The overlay correction" (word-count is NOT confidence)
    - 02-CONTEXT.md D-03 AMENDED (uniform heuristic; overlay = word count only, kept internal)
  </read_first>
  <action>
    In lib/providers/ocrspace.js: (1) set `form.append('isOverlayRequired','true')` (was 'false'). (2) Accept an optional `opts` argument carrying `opts.signal`; replace the hardcoded `signal: AbortSignal.timeout(2*60*1000)` in the fetch call with a composed signal — when a caller signal is present use `AbortSignal.any([opts.signal, AbortSignal.timeout(cap)])`, else keep the timeout-only fallback (cap stays the existing 2-min backstop). (3) After the existing IsErroredOnProcessing guard, walk `data.ParsedResults` accumulating `wordCount += sum(TextOverlay.Lines[].Words.length)` and `hasOverlay = any(TextOverlay.HasOverlay)`; return `{ ok:true, timeMs, text, overlay:{ HasOverlay:hasOverlay, wordCount }, ocrExitCode: data.OCRExitCode }`. Do NOT return raw geometry (PROJECT/REQUIREMENTS forbid exposing overlay in the public API — it stays internal to the heuristic). Do NOT fabricate a confidence scalar. Keep the existing non-JSON-body and ErrorMessage-array normalization guards intact. On an AbortError from fetch, keep returning `{ ok:false, error }` (the catch already does this via toString) so the runner sees a fall-through, not a throw.
  </action>
  <acceptance_criteria>
    - grep confirms overlay is enabled: `grep -c "isOverlayRequired','true'" lib/providers/ocrspace.js` returns 1 and the old `'false'` literal is gone.
    - The success return object includes `overlay.wordCount`, `overlay.HasOverlay`, and `ocrExitCode` (verified by the Task 3 test with a mocked fetch).
    - No raw `Words`/`Left`/`Top` geometry is present in the return object: `grep -cE "return[^;]*Left|return[^;]*WordText" lib/providers/ocrspace.js` returns 0.
    - runOcrSpace accepts an opts/signal parameter (signature widened).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('./lib/providers/ocrspace'); process.exit(typeof s.runOcrSpace==='function'?0:1)" && grep -q "isOverlayRequired','true'" lib/providers/ocrspace.js && echo OK</automated>
  </verify>
  <done>ocr.space enables overlay, returns {text, overlay:{HasOverlay,wordCount}, ocrExitCode}, honors an external abort signal, and never exposes raw geometry or a fabricated confidence.</done>
</task>

<task type="auto">
  <name>Task 2: Thread signal through ollama and runOCR</name>
  <files>lib/providers/ollama.js, lib/ocr.js</files>
  <read_first>
    - lib/providers/ollama.js (axios.post config — add signal; existing ECONNABORTED handling to preserve)
    - lib/ocr.js (runOCR dispatch — must forward opts including signal to both providers)
    - 02-RESEARCH.md §"Single authoritative deadline (JOB-04)" and §"Provider changes to accept signal" (axios abort → ERR_CANCELED → hard-failure fall-through)
  </read_first>
  <action>
    In lib/providers/ollama.js: pass `signal: opts?.signal` in the axios.post config object (axios honors AbortController.signal); keep the existing 5-min `timeout` as a backstop only. Extend the catch so an aborted call (`err.code === 'ERR_CANCELED'` or `err.name === 'CanceledError'`) returns `{ ok:false, error:'Cancelado por deadline del job', status: err.response?.status }` (a hard failure the runner falls through on), preserving the existing ECONNABORTED and generic branches. In lib/ocr.js: forward the full `opts` (which now may carry `signal`) into runOcrSpace — call `runOcrSpace(model.engine, model.language, base64Image, mimeType, apiKey, opts)` — and into runOllama (already receives opts). Do not change runOCR's public arg order (model, base64, mimeType, apiKey, opts) — Phase-1 callers rely on it.
  </action>
  <acceptance_criteria>
    - `node -e "const o=require('./lib/ocr'); process.exit(typeof o.runOCR==='function'?0:1)"` exits 0.
    - grep confirms ollama forwards the signal: `grep -c "signal" lib/providers/ollama.js` >= 1.
    - lib/ocr.js passes opts to runOcrSpace: `grep -cE "runOcrSpace\\([^)]*opts" lib/ocr.js` returns 1.
    - Existing Phase-1 worker/route tests that call runOCR without a signal still pass (signal is optional).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "signal" lib/providers/ollama.js && grep -Eq "runOcrSpace\([^)]*opts" lib/ocr.js && node -e "require('./lib/ocr'); require('./lib/providers/ollama')" && echo OK</automated>
  </verify>
  <done>runOCR forwards opts.signal to both providers; ollama passes it to axios and maps abort to a clean ok:false fall-through without breaking the Phase-1 call sites.</done>
</task>

<task type="auto">
  <name>Task 3: Unit-test abort propagation and ocr.space overlay shape</name>
  <files>test/provider-signal.test.js</files>
  <read_first>
    - test/worker.test.js and test/worker-logging.test.js (require.cache monkey-patch technique for isolating a module under test without network)
    - lib/providers/ocrspace.js and lib/providers/ollama.js (return shapes to assert) — modified in Tasks 1-2
    - 02-RESEARCH.md §"Environment Availability" (unit tests are pure — mock fetch/axios, no keys)
  </read_first>
  <action>
    Write test/provider-signal.test.js as a `node:test` suite with NO network and NO keys. (a) ocr.space overlay shape: stub `global.fetch` to resolve a fake ocr.space JSON body (ParsedResults with a TextOverlay.Lines[].Words array of N entries, HasOverlay:true, OCRExitCode:1); call runOcrSpace and assert the return has `overlay.wordCount === N`, `overlay.HasOverlay === true`, `ocrExitCode === 1`, and no raw geometry keys; restore fetch after. (b) ocr.space OCRExitCode:3 (all pages failed) is returned so the runner can branch. (c) ocr.space abort: pass an already-aborted AbortSignal (or stub fetch to reject with an AbortError) and assert runOcrSpace resolves to `{ ok:false }` rather than throwing. (d) ollama abort: monkey-patch axios.post (via require.cache) to reject with `{ code:'ERR_CANCELED' }` and assert runOllama resolves `{ ok:false }` with a non-crashing error string. Keep each stub scoped with t.after restore.
  </action>
  <acceptance_criteria>
    - `node --test test/provider-signal.test.js` exits 0.
    - The suite asserts overlay.wordCount equals the mocked word count and that an aborted call yields ok:false (not a thrown error).
    - No real network: the suite stubs global.fetch and axios.post (grep confirms neither a real api.ocr.space nor ollama.com request escapes — no un-stubbed provider call).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/provider-signal.test.js</automated>
  </verify>
  <done>Provider-level abort and overlay-extraction behavior is proven with mocked HTTP clients; aborts fall through as ok:false and overlay.wordCount is populated for the heuristic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → ocr.space / ollama.com | outbound calls whose responses (text, error bodies) are untrusted input |
| provider error body → logs/trace | raw provider error strings must not leak keys/PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | hung provider socket wedging single-concurrency worker | mitigate | Thread job AbortSignal into fetch (composed via AbortSignal.any) and axios {signal}; per-engine timeout subordinate to job deadline (Pitfall 3) |
| T-02-05 | Information Disclosure | provider error body in return.error | mitigate | Continue normalizing via existing toString/ErrorMessage guards; return code/short strings only, never raw bodies/keys (OPS-05) |
| T-02-06 | Spoofing/Tampering | ocr.space geometry mistaken for confidence | mitigate | Return overlay.wordCount only (a count), never fabricate a confidence scalar (D-03 amended, Pitfall 1) |
| T-02-07 | Tampering | npm/pip/cargo installs | accept | Zero new packages (native fetch/AbortSignal + installed axios); no install surface (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node --test test/provider-signal.test.js` exits 0.
- ocr.space enables overlay and returns a scalar wordCount + ocrExitCode; no raw geometry or fabricated confidence.
- Both providers accept and honor an AbortSignal; aborts fall through as ok:false.
- Existing Phase-1 provider/worker call sites still work (signal is optional; arg order unchanged).
</verification>

<success_criteria>
- A single authoritative signal can abort either provider mid-call (JOB-04 prerequisite).
- ocr.space contributes the overlay word-count signal (CASC-03 Signal C) the heuristic consumes.
- No Phase-1 regression: runOCR's contract stays backward-compatible.
</success_criteria>

<output>
Create `.planning/phases/02-cascade-router/02-02-SUMMARY.md` when done.
Note whether live-key confirmation of ocr.space overlay behavior is still an open ops item (calibration deferral).
</output>
