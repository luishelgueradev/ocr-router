---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - test/auth.test.js
  - test/concurrency.test.js
  - test/env-guards.test.js
  - test/errors.test.js
  - test/health.test.js
  - test/jobs-extra.test.js
  - test/logger-shape.test.js
  - test/models-shape.test.js
  - test/modes.test.js
  - test/pino-http.test.js
  - test/shutdown.test.js
  - test/sniff.test.js
  - test/upload.test.js
  - test/worker.test.js
  - test/worker-logging.test.js
  - test/worker-failure-logging.test.js
  - scripts/verify-redaction.js
autonomous: true
requirements: [API-01, API-02, API-07, API-08, JOB-01, JOB-03, INP-01, INP-02, OPS-04, OPS-05]
must_haves:
  truths:
    - "The full ported node --test suite (minus deploy.test.js) passes against the Plan 01 app"
    - "shutdown.test.js seeds and asserts the terminal status `succeeded`, and drain leaves succeeded/failed jobs untouched while cancelling processing jobs"
    - "worker success-path tests assert the page-aware envelope (result.pages[0]); failure-path tests still assert status:failed"
    - "verify-redaction.js proves secrets (authorization / apiKeyOverride / API keys) never appear in structured logs"
    - "env-guards.test.js proves the API_TOKEN + TAILSCALE_IP placeholder-drift guards fail closed"
  artifacts:
    - path: "test/shutdown.test.js"
      provides: "SIGTERM drain semantics test with succeeded status (D-03)"
      contains: "succeeded"
    - path: "test/worker.test.js"
      provides: "Worker terminal-state + envelope assertions"
      contains: "pages"
    - path: "scripts/verify-redaction.js"
      provides: "Log-redaction verification (OPS-05)"
  key_links:
    - from: "test/shutdown.test.js"
      to: "lib/v1/jobs.js"
      via: "seeds status:'succeeded' and drives drainAndCancel"
      pattern: "succeeded"
    - from: "test/worker.test.js"
      to: "lib/v1/worker.js"
      via: "asserts jobs.complete envelope shape"
      pattern: "pages"
---

<objective>
Port and adapt the remaining reference `node --test` suite (16 test files + `scripts/verify-redaction.js`) so the full acceptance proof for Phase 1 runs green against the Plan 01 application. These tests are the acceptance evidence for the ~14 already-implemented requirements (auth, env guards, upload limits, magic-byte sniff, concurrency/backpressure, structured logging, redaction, SIGTERM drain).

Most files port AS-IS (no status/field/envelope coupling). Three files require D-03/D-04 adaptations: `shutdown.test.js` (status literal), `worker.test.js` (success-path envelope), and the worker-logging pair (verify only). This plan touches ONLY test files and `scripts/` — no application code — so it runs in parallel with the deploy plan (01-03).

Purpose: Lock in the reliability guarantees the reference already encodes. Output: a fully green test suite (minus deploy.test.js, owned by 01-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
lib/v1/jobs.js exports: { create, get, setProcessing, complete, fail, iterateAll, _clearForTest }
  - terminal statuses: 'succeeded' (renamed from 'completed'), 'failed'
lib/v1/shutdown.js exports: { drainAndCancel } — cancels 'processing' jobs, leaves terminal jobs untouched
lib/v1/worker.js exports: { runJob, limiter, MAX_QUEUE_DEPTH }
  - complete payload: { text, pages:[{page,text,engine,confidence}], engine, provider, mode, bytes_received }
scripts/verify-redaction.js: asserts pino redact removes authorization/apiKeyOverride/API keys from logs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the AS-IS test files + verify-redaction (no adaptation)</name>
  <files>test/auth.test.js, test/concurrency.test.js, test/env-guards.test.js, test/errors.test.js, test/health.test.js, test/logger-shape.test.js, test/models-shape.test.js, test/modes.test.js, test/pino-http.test.js, test/sniff.test.js, test/upload.test.js, scripts/verify-redaction.js</files>
  <read_first>
    - REFERENCE: /home/luis/proyectos/test-ocr-qwen3-vl/test/{auth,concurrency,env-guards,errors,health,logger-shape,models-shape,modes,pino-http,sniff,upload}.test.js
    - REFERENCE: /home/luis/proyectos/test-ocr-qwen3-vl/scripts/verify-redaction.js
    - 01-RESEARCH.md §Tests table (the AS-IS (verify) row) and Pitfall 5 (env-guards drift guard byte-coupling)
    - lib/v1/* (this repo, from 01-01) to confirm imports resolve
  </read_first>
  <action>
    Copy each listed file AS-IS from the reference. These have no `completed`/`image`/envelope coupling per the grep-enumerated RESEARCH audit. Two files need a confirm-not-edit pass: `upload.test.js` — check for any `'image'` field literal and rename to `'file'` only if present (the fileFilter field name changed in 01-01); `env-guards.test.js` — the placeholder-drift guard byte-couples `server.js` PLACEHOLDER_API_TOKEN/PLACEHOLDER_TAILSCALE_IP to `.env.example`; do not alter the literals. Run each file to confirm green against the ported app.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/ocr && node --test test/auth.test.js test/concurrency.test.js test/env-guards.test.js test/errors.test.js test/health.test.js test/logger-shape.test.js test/models-shape.test.js test/modes.test.js test/pino-http.test.js test/sniff.test.js test/upload.test.js && node --test scripts/verify-redaction.js</automated>
  </verify>
  <acceptance_criteria>
    - The listed `node --test` invocation exits 0 (all files green).
    - `node --test scripts/verify-redaction.js` exits 0 — no secret string appears in emitted logs.
    - `grep -v '^\s*//' test/upload.test.js | grep -c "'image'"` returns 0 (field renamed if it was present).
    - env-guards.test.js asserts boot rejection when API_TOKEN or TAILSCALE_IP equals its placeholder.
  </acceptance_criteria>
  <done>All non-adapted tests + redaction verification pass against the ported app, proving auth, env-guards, limits, sniff, concurrency, and logging requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapt the status/envelope-coupled tests (shutdown, worker, jobs-extra, worker-logging pair)</name>
  <files>test/shutdown.test.js, test/worker.test.js, test/jobs-extra.test.js, test/worker-logging.test.js, test/worker-failure-logging.test.js</files>
  <read_first>
    - REFERENCE: /home/luis/proyectos/test-ocr-qwen3-vl/test/shutdown.test.js (lines 52/57/58 status literal), test/worker.test.js, test/jobs-extra.test.js (lines 97/98), test/worker-logging.test.js, test/worker-failure-logging.test.js
    - lib/v1/shutdown.js, lib/v1/worker.js, lib/v1/jobs.js (this repo, from 01-01)
    - 01-RESEARCH.md §Tests table (EDIT rows) + §"Adaptation #1" and §"Adaptation #2"
    - 01-CONTEXT.md D-03, D-04
  </read_first>
  <behavior>
    - shutdown.test.js seeds jobs with status `succeeded` and asserts drain leaves them untouched while cancelling `processing` jobs.
    - worker.test.js success path asserts jobs.complete produced `result.pages[0]` (page:1, engine, confidence:null) + `result.text`; failure path asserts status `failed`.
    - jobs-extra.test.js terminal-status assertion is `succeeded`; result assertion reads `result.text` from the envelope.
    - worker-logging / worker-failure-logging assert the 'job complete' / 'job failed' log shapes unchanged.
  </behavior>
  <action>
    Port these five files with the D-03/D-04 edits: in `shutdown.test.js`, change the seeded/asserted terminal status `'completed'` to `'succeeded'` (lines ~52/57/58). In `jobs-extra.test.js`, change the terminal status literal (line ~97) to `'succeeded'` and update the result assertion (line ~98) to the envelope `result.text`. In `worker.test.js`, if it exercises the success path, update assertions to the page-aware envelope (`result.pages[0]`); keep failure-path `status:'failed'` unchanged. For `worker-logging.test.js` and `worker-failure-logging.test.js`, verify against the ported worker and update ONLY if they assert a result shape or status literal (the 'job complete' log line was intentionally left unchanged in 01-01, so log-shape assertions should still pass). Run all five green.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/ocr && node --test test/shutdown.test.js test/worker.test.js test/jobs-extra.test.js test/worker-logging.test.js test/worker-failure-logging.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The five-file `node --test` invocation exits 0.
    - `grep -c "succeeded" test/shutdown.test.js` ≥ 1; `grep -v '^\s*//' test/shutdown.test.js | grep -c "'completed'"` returns 0.
    - `grep -v '^\s*//' test/jobs-extra.test.js | grep -c "'completed'"` returns 0.
    - worker.test.js success assertions reference `pages` (`grep -c pages test/worker.test.js` ≥ 1) OR the file exercises only the failure path (documented in SUMMARY).
  </acceptance_criteria>
  <done>All status/envelope-coupled tests pass with the `succeeded` vocabulary and page-aware envelope; drain semantics and worker logging remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same boundaries as 01-01 (client→/v1, tailnet→admin, app→provider). This plan adds no runtime surface; it verifies mitigations.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Info disclosure | log secret leakage | mitigate | scripts/verify-redaction.js asserts redaction (this plan proves it) |
| T-01-06 | Elevation | boot without token / placeholder | mitigate | env-guards.test.js proves fail-closed drift guards |
| T-01-04 | DoS | queue overflow | mitigate | concurrency.test.js proves 503 backpressure |
| T-01-SC | Tampering | npm installs | accept | No new packages introduced in this plan |
</threat_model>

<verification>
- `npm test` runs green EXCEPT `test/deploy.test.js` (created by 01-03). Until 01-03 lands, verify with the explicit file lists in the two tasks above.
- After both wave-2 plans complete, the full `npm test` script passes end-to-end (phase-level gate).
</verification>

<success_criteria>
- Full ported test suite (16 files + verify-redaction) green against the Plan 01 app.
- D-03/D-04 adaptations applied to the status/envelope-coupled tests.
- No application code modified (test/ and scripts/ only) — clean parallel with 01-03.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>
