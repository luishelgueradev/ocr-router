---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Dockerfile
  - docker-compose.yml
  - Caddyfile
  - test/deploy.test.js
  - README.md
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03, OPS-04, OPS-05]
must_haves:
  truths:
    - "The Docker image builds on node:22-bookworm-slim with poppler-utils + tini installed via apt-get (no PDF code yet)"
    - "tini runs as ENTRYPOINT at the Debian path /usr/bin/tini; CMD is exec-form [node, server.js]"
    - "The compose healthcheck uses a Node-native fetch probe (bookworm-slim has no wget/curl)"
    - "Caddy fronts the app with automatic HTTPS, default-denies everything except /v1/*, and the app binds only to ${TAILSCALE_IP}:8780:3000, never 0.0.0.0"
    - "In-flight jobs drain on SIGTERM within stop_grace_period 40s (5s buffer over the 35s app drain)"
    - "deploy.test.js asserts the Debian/bookworm image shape, poppler-utils install, /usr/bin/tini path, and the node-native healthcheck — passing in lockstep with the rewritten Dockerfile/compose"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage node:22-bookworm-slim image with tini + poppler-utils"
      contains: "node:22-bookworm-slim"
    - path: "docker-compose.yml"
      provides: "Two-service stack (ocr-app tailnet-bound + ocr-caddy public) with node-native healthcheck"
      contains: "stop_grace_period"
    - path: "Caddyfile"
      provides: "Default-deny reverse proxy, only /v1/* public, automatic HTTPS"
      contains: "handle"
    - path: "test/deploy.test.js"
      provides: "Static deploy-artifact assertions for the Debian migration"
      contains: "node:22-bookworm-slim"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build.dockerfile references the Debian image"
      pattern: "dockerfile:\\s*Dockerfile"
    - from: "docker-compose.yml"
      to: "Caddyfile"
      via: "caddy service mounts ./Caddyfile read-only"
      pattern: "Caddyfile:/etc/caddy"
    - from: "test/deploy.test.js"
      to: "Dockerfile"
      via: "reads and asserts node:22-bookworm-slim + poppler-utils + /usr/bin/tini"
      pattern: "poppler-utils"
---

<objective>
Migrate the reference deploy stack from Alpine/Node 20 to the pinned `node:22-bookworm-slim` base with `poppler-utils` + `tini`, and rewrite `test/deploy.test.js` in lockstep (its assertions are byte-coupled to the old Alpine Dockerfile). Port the Caddyfile AS-IS and adapt `docker-compose.yml` for the two Debian-specific pitfalls (busybox `wget` healthcheck → Node-native `fetch` probe; image tag → `ocr-router:latest`).

This is the deploy leg of the walking skeleton: `docker compose build` succeeds, the app binds Tailscale-only behind Caddy with automatic HTTPS, and SIGTERM drains in-flight jobs. `poppler-utils` is installed now per D-10 (needed in Phase 3) but NO PDF/sharp code is added. This plan touches only deploy artifacts + `deploy.test.js` + README — no application or lib code — so it runs in parallel with the test-suite plan (01-02).

Purpose: Produce a buildable, deployable, Tailscale-isolated stack on the correct base image. Output: Dockerfile, docker-compose.yml, Caddyfile, deploy.test.js, README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Deploy contract constants (from reference + RESEARCH pitfalls). -->
Base image: node:22-bookworm-slim (was node:20-alpine)
System packages (runtime stage): apt-get install -y --no-install-recommends tini poppler-utils
tini path: /usr/bin/tini  (Debian; was /sbin/tini on Alpine)
Healthcheck probe: node -e "fetch('http://localhost:3000/v1/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
App port bind: ${TAILSCALE_IP:?...}:8780:3000  (never 0.0.0.0)
stop_grace_period: 40s ; app SIGTERM drain budget: 35s (server.js gracefulShutdown)
Image tag: ocr-router:latest (was test-ocr-qwen3-vl:latest)
Caddy: default-deny, only /v1/* proxied, {$DOMAIN} automatic HTTPS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Dockerfile for node:22-bookworm-slim + poppler-utils + tini; port Caddyfile; adapt compose</name>
  <files>Dockerfile, Caddyfile, docker-compose.yml</files>
  <read_first>
    - REFERENCE (REWRITE): /home/luis/proyectos/test-ocr-qwen3-vl/Dockerfile (Alpine/Node20 — the migration source)
    - REFERENCE (AS-IS): /home/luis/proyectos/test-ocr-qwen3-vl/Caddyfile
    - REFERENCE (EDIT): /home/luis/proyectos/test-ocr-qwen3-vl/docker-compose.yml (healthcheck line 52; image tag line 29)
    - 01-RESEARCH.md §"Common Pitfalls" Pitfall 1 (Alpine to Debian tini path + apk to apt-get) and Pitfall 2 (busybox wget absent, use node fetch probe); §Port Map entry-point rows; §Stack system packages
    - 01-CONTEXT.md D-10 (node:22-bookworm-slim + poppler-utils + tini now, no PDF code), D-09 (admin tailnet-bound)
  </read_first>
  <action>
    Rewrite `Dockerfile`: keep the `# syntax=docker/dockerfile:1.6` first line and the two-stage (deps + runtime) structure, but change both `FROM node:20-alpine` stages to `FROM node:22-bookworm-slim`. Replace `RUN apk add --no-cache tini` with an apt-get install of `tini poppler-utils` using `--no-install-recommends` and a trailing `rm -rf /var/lib/apt/lists/*` in the runtime stage. Change `ENTRYPOINT ["/sbin/tini", "--"]` to `ENTRYPOINT ["/usr/bin/tini", "--"]` (Debian path). Keep `npm ci --omit=dev`, `USER node`, `ENV NODE_ENV=production`, `EXPOSE 3000`, and the exec-form `CMD ["node", "server.js"]`. Add NO HEALTHCHECK instruction (it lives in compose per D-04). Add NO sharp/PDF code — poppler is installed but unused this phase.

    Copy `Caddyfile` AS-IS from the reference — default-deny, four `/v1/*` handle blocks with per-route timeouts + `dial_timeout 2s`, `{$DOMAIN}` automatic HTTPS, terminal `handle { error 404 }`, commented `acme_ca` staging hint. Update only project-name comments if present; do not add header_up/header_down/log_credentials.

    Edit `docker-compose.yml`: replace the healthcheck test with the Node-native probe (CMD-SHELL running `node -e` with a global `fetch` to `http://localhost:3000/v1/health` exiting 0 on ok else 1) per Pitfall 2. Change the app `image:` tag from `test-ocr-qwen3-vl:latest` to `ocr-router:latest`. Keep everything else AS-IS: the `${TAILSCALE_IP:?...}:8780:3000` bind, `stop_grace_period: 40s`, `NODE_ENV=production`, no `depends_on` on app, caddy `caddy:2-alpine` with cert volumes + `depends_on.app.condition: service_healthy`, `ocr_net` bridge, `caddy_data`/`caddy_config` volumes.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/ocr && grep -q "node:22-bookworm-slim" Dockerfile && grep -q "poppler-utils" Dockerfile && grep -q "/usr/bin/tini" Dockerfile && grep -q "v1/health" docker-compose.yml && grep -q "ocr-router:latest" docker-compose.yml && echo DEPLOY-ARTIFACTS-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "node:22-bookworm-slim" Dockerfile` returns 2 (both stages); `grep -c "node:20-alpine" Dockerfile` returns 0.
    - Dockerfile installs `tini poppler-utils` via apt-get and uses `ENTRYPOINT ["/usr/bin/tini", "--"]`; NO `apk` and NO `/sbin/tini`.
    - `grep -c HEALTHCHECK Dockerfile` returns 0.
    - `docker-compose.yml` healthcheck uses a `node -e` fetch probe (no `wget`/`curl`); app `image:` is `ocr-router:latest`; app port bind matches `${TAILSCALE_IP...}:8780:3000`.
    - Caddyfile has exactly 4 `reverse_proxy app:3000` directives and a terminal `handle { error 404 }`.
  </acceptance_criteria>
  <done>The deploy artifacts target the correct Debian base with poppler-utils + tini, a working node-native healthcheck, and the Tailscale-only + default-deny network model.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite deploy.test.js for the Debian migration; smoke-build the image; write README</name>
  <files>test/deploy.test.js, README.md</files>
  <read_first>
    - REFERENCE (REWRITE): /home/luis/proyectos/test-ocr-qwen3-vl/test/deploy.test.js (DEPLOY-01 stage/tini/entrypoint assertions; DEPLOY-07 wget healthcheck assertions — all Alpine-coupled)
    - Dockerfile, docker-compose.yml, Caddyfile (this repo, from Task 1)
    - 01-RESEARCH.md §"N-3 deploy.test.js rewrite" and Pitfalls 1-2; §"Environment Availability" (Docker build is the build gate)
    - 01-CONTEXT.md D-10, D-11 (build = docker compose build; node --test is the runner)
  </read_first>
  <action>
    Rewrite `test/deploy.test.js` so every assertion matches the Task-1 artifacts, updating in lockstep: DEPLOY-01 stage regex `FROM node:20-alpine AS (deps|runtime)` becomes `FROM node:22-bookworm-slim AS (deps|runtime)`; the `apk add --no-cache tini` assertion becomes an apt-get `tini poppler-utils` install assertion (add a dedicated assertion that `poppler-utils` is installed); the ENTRYPOINT regex `/sbin/tini` becomes `/usr/bin/tini`. DEPLOY-07 healthcheck regex changes from `wget --spider ...` to the node fetch probe; keep the interval/timeout/retries/start_period assertions and the "no HEALTHCHECK in Dockerfile" assertion. Keep DEPLOY-03 (compose two-service shape, TAILSCALE bind, stop_grace_period 40s, no depends_on on app, caddy volumes/ports) and DEPLOY-04 (Caddyfile default-deny) AS-IS — but update the image-tag expectation if asserted to `ocr-router`. Keep DEPLOY-06 `.env.example` assertions AS-IS (placeholders unchanged).

    Run `node --test test/deploy.test.js` green. Then run a `docker compose build` (or `docker build`) smoke check to confirm the Debian image builds and tini resolves at `/usr/bin/tini` (Pitfalls 1-2 flagged for build smoke). Write a concise `README.md` for ocr-router: what it is, required env vars, `docker compose build` / run, and the local `node server.js` dev path.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/ocr && node --test test/deploy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/deploy.test.js` exits 0.
    - `grep -c "node:22-bookworm-slim" test/deploy.test.js` ≥ 1; `grep -c "node:20-alpine" test/deploy.test.js` returns 0; `grep -c "/usr/bin/tini" test/deploy.test.js` ≥ 1; `grep -c "poppler-utils" test/deploy.test.js` ≥ 1.
    - deploy.test.js healthcheck assertion references the node fetch probe, not `wget`.
    - `docker compose build` (or `docker build .`) completes successfully (recorded in SUMMARY as the build gate); if Docker is unavailable on the host, record the blocker and the exact command to run on the build host.
    - `README.md` documents required env vars and the build/run + local-dev commands.
  </acceptance_criteria>
  <done>deploy.test.js passes against the migrated artifacts, the image builds on the Debian base, and README documents the stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → Caddy | TLS termination; only /v1/* proxied, everything else 404 |
| Caddy → app (docker bridge) | internal; app never published on 0.0.0.0 |
| tailnet → admin surface (${TAILSCALE_IP}:8780) | tailnet is the trust perimeter (D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Elevation | admin panel public exposure | mitigate | compose binds `${TAILSCALE_IP:?...}:8780:3000` (fail-loud if unset) + Caddy default-deny; deploy.test.js asserts no 0.0.0.0 / plain 3000:3000 |
| T-01-08 | Info disclosure | TLS + cert persistence | mitigate | Caddy automatic HTTPS; caddy_data/caddy_config volumes persist certs (Pitfall A) |
| T-01-09 | DoS | container won't start (wrong tini path / no wget) | mitigate | /usr/bin/tini Debian path + node-native healthcheck (Pitfalls 1-2); docker build smoke gate |
| T-01-10 | Tampering | Caddy log-credential leak | mitigate | no `log_credentials true`; Caddy 2.5+ auto-redaction; deploy.test.js asserts absence |
| T-01-SC | Tampering | apt/npm installs | accept | poppler-utils/tini are Debian base packages; no new npm packages (RESEARCH audit: none flagged) |
</threat_model>

<verification>
- `node --test test/deploy.test.js` exits 0 against the migrated artifacts.
- `docker compose build` succeeds on node:22-bookworm-slim (the D-11 build gate); container ENTRYPOINT resolves `/usr/bin/tini`.
- Phase-level gate (after 01-02 + 01-03): full `npm test` passes (all 19 files + verify-redaction green).
</verification>

<success_criteria>
- Dockerfile on node:22-bookworm-slim with tini + poppler-utils at Debian paths; no Alpine remnants.
- Compose: node-native healthcheck, Tailscale-only bind, 40s grace, Caddy default-deny public surface.
- deploy.test.js rewritten and green in lockstep; image builds; README written.
- No application/lib/test-suite code touched (clean parallel with 01-02).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>
