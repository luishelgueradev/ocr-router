---
phase: 03-input-pipeline
plan: 07
type: execute
wave: 4
depends_on: ["03-06"]
files_modified:
  - test/docker-smoke.test.js
  - scripts/docker-smoke.sh
  - README.md
autonomous: false
requirements: [INP-04, INP-05, INP-08]
must_haves:
  truths:
    - "A real-poppler + real-HEIC integration smoke runs GREEN inside the Docker image (the two STATE risk-flags validated), and is SKIP-GUARDED on the host so `npm test` never depends on poppler/HEIC"
    - "The smoke proves: a scanned PDF rasterizes and OCRs page-by-page, a HEIC decodes in Docker, the ulimit/kill sandbox terminates a runaway/aborted child, and a mid-job SIGTERM leaves no temp dir"
  artifacts:
    - path: "test/docker-smoke.test.js"
      provides: "Skip-guarded real-dependency integration checks (poppler present + HEIC decode + temp cleanup)"
    - path: "scripts/docker-smoke.sh"
      provides: "Runs the smoke inside the built image"
  key_links:
    - from: "test/docker-smoke.test.js"
      to: "pdftoppm availability"
      via: "skip-guard when the binary is absent (host)"
      pattern: "skip|which pdftoppm"
---

<objective>
Validate the two Docker-only STATE risk-flags — subprocess-sandbox mechanics (ulimit/kill on bookworm-slim) and HEIC-in-Docker decode — with a skip-guarded integration smoke that runs GREEN inside the image and SKIPS on the host, so host `npm test` never depends on real poppler/HEIC (D-11). This is the recorded Docker/human smoke, not a host-suite gate.

Purpose: The seam + caps are host-unit-tested, but the real ulimit numbers (A1) and HEIC decode correctness (A5) can only be proven against the deployed base image. This plan turns those assumptions into an executed, recorded check.
Output: `test/docker-smoke.test.js` (skip-guarded), `scripts/docker-smoke.sh`, README run instructions, and a human-verified pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-input-pipeline/03-RESEARCH.md
@.planning/phases/03-input-pipeline/03-CONTEXT.md
@Dockerfile
@lib/v1/input/rasterize.js
@lib/v1/input/image-normalize.js
@lib/v1/input/spawn-capture.js
@lib/v1/input/temp.js

<interfaces>
<!-- The real-dependency modules to exercise unstubbed inside Docker -->
renderPage(pdfPath, page, { dpi, maxDim, signal }) -> Promise<Buffer>   // real pdftoppm
pdfPageCount(pdfPath, { signal }) -> Promise<number>                    // real pdfinfo
normalizeToFrames(heicBuffer, 'image/heic', {}) -> Promise<Buffer[]>    // real heic-convert
spawnCapture(...)  // real sh/ulimit/timeout kill behavior
createJobTempDir/cleanupJobTempDir/drainAllTempDirs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skip-guarded real-dependency integration smoke + runner</name>
  <files>test/docker-smoke.test.js, scripts/docker-smoke.sh, README.md</files>
  <read_first>
    - 03-RESEARCH.md §"Code Examples" → "Testability seam (D-11)" (the skip-guard: `if (!which('pdftoppm')) t.skip('poppler Docker-only')`) and §"Environment Availability" (poppler host-absent by design; timeout/sh/ulimit in the Debian base)
    - 03-RESEARCH.md §"Assumptions Log" A1 (ulimit -v sizing), A5 (HEIC-in-Docker), A6 (timeout/sh/ulimit present) and §"Pitfall 2"/"Pitfall 4" (mid-job SIGTERM temp drain; exec-or-orphan) — these are exactly what the smoke must prove
    - Dockerfile (poppler-utils + tini already installed; this smoke runs in that runtime image) and lib/v1/input/{rasterize,image-normalize,spawn-capture,temp}.js (exercised unstubbed)
  </read_first>
  <action>
    Write test/docker-smoke.test.js using `node --test` with a skip-guard at the top of each real-dependency test: detect `pdftoppm` on PATH (e.g. spawn `which pdftoppm` or check exit) and `t.skip('poppler Docker-only')` when absent, so the file is GREEN-by-skip on the host and executes for real in the image. Cover, unstubbed: (1) pdfPageCount + renderPage against a small real scanned-PDF fixture produce a valid PNG buffer per page; (2) normalizeToFrames on a real HEIC fixture returns a normalized PNG (STATE A5); (3) a spawnCapture invocation of a deliberately long-running command aborted via signal is terminated within the grace window (SIGKILL escalation + `exec` reaches the child — A1/Pitfall 4); (4) a job that creates a temp dir then simulates a mid-job SIGTERM (drainAllTempDirs) leaves no temp dir on disk (Pitfall 2). Add scripts/docker-smoke.sh that builds the image (or uses the existing tag) and runs `node --test test/docker-smoke.test.js` inside a container. Do NOT add test/docker-smoke.test.js to the host `npm test` script in package.json — it must never gate the host suite (D-11); document it as a Docker/human smoke in README with the exact command and the assumptions it validates (A1, A5, A6).
  </action>
  <verify>
    <automated>node --test test/docker-smoke.test.js</automated>
  </verify>
  <acceptance_criteria>
    - On the host, `node --test test/docker-smoke.test.js` exits 0 with the real-dependency cases SKIPPED (not failed) — poppler/HEIC absent
    - test/docker-smoke.test.js is NOT referenced in package.json's `test` script
    - README documents the Docker smoke command + the risk-flags (A1 ulimit sizing, A5 HEIC-in-Docker) it validates
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A skip-guarded Docker integration smoke that exercises real poppler rasterization, real HEIC decode, the ulimit/kill subprocess sandbox, and mid-job temp cleanup inside the built image — validating the two STATE Phase-3 risk-flags that cannot be proven on the host.</what-built>
  <how-to-verify>
    1. Build the image: `docker compose build` (or `docker build -t ocr-router:latest .`).
    2. Run the smoke inside the image: `bash scripts/docker-smoke.sh` (runs `node --test test/docker-smoke.test.js` in a container).
    3. Confirm ALL real-dependency cases now EXECUTE (not skip) and pass: scanned-PDF page rasterizes to a PNG, HEIC decodes to a normalized PNG, the aborted subprocess is killed within the grace window, and the temp dir is gone after the simulated SIGTERM.
    4. If the ulimit -v number is too tight (a legitimate large page fails to render) or too loose, note the value and adjust CAPS.ULIMIT_V_KB via env; record the confirmed value.
  </how-to-verify>
  <acceptance_criteria>
    - The Docker smoke runs GREEN inside the image with the real-dependency cases executed (not skipped)
    - The confirmed ulimit -v value and HEIC-decode result are recorded in the SUMMARY (closing A1/A5)
  </acceptance_criteria>
  <resume-signal>Type "approved" (with the confirmed ulimit value + HEIC result) or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deployed image → real subprocess/decoders | The only place ulimit/kill + HEIC decode run against the real base image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | DoS (ulimit mis-sized on real image) | spawnCapture in Docker | mitigate | Docker smoke validates the actual ulimit -v number rejects a bomb while allowing a legitimate page (A1); value recorded + env-tunable |
| T-03-20 | Tampering (HEIC decoder fails/misbehaves in Docker) | heic-convert in Docker | mitigate | Docker smoke proves real HEIC decode correctness against the runtime image (A5) — the STATE risk-flag |
</threat_model>

<verification>
- Host: file green-by-skip, not in the `npm test` gate.
- Docker: all real-dependency cases execute and pass; ulimit value + HEIC result recorded.
</verification>

<success_criteria>
The two STATE Phase-3 risk-flags (subprocess-sandbox mechanics, HEIC-in-Docker) are validated by an executed Docker smoke, recorded, and kept out of the host suite per D-11.
</success_criteria>

<output>
Create `.planning/phases/03-input-pipeline/03-07-SUMMARY.md` when done (include the confirmed ulimit -v value + HEIC decode result).
</output>
