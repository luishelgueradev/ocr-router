---
phase: 03-input-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/v1/input/caps.js
  - test/caps.test.js
  - README.md
autonomous: true
requirements: [OPS-06, INP-07]
must_haves:
  truths:
    - "The four new decoders (unpdf, sharp>=0.35.0, heic-convert, @vingle/bmp-js) are installed at the CVE-fixed pins and require() / import() succeed on the host"
    - "npm run audit exits 0 (no high+ advisories in production deps)"
    - "Every memory cap (pages, DPI, pixels, ulimit, job budget, native-char floor) is a validated env-overridable config value with a defensible default"
  artifacts:
    - path: "lib/v1/input/caps.js"
      provides: "Env-driven, boot-validated caps (MAX_PDF_PAGES/RASTER_DPI/MAX_OUTPUT_PIXELS/ULIMIT_V_KB/ULIMIT_CPU_SEC/RASTER_WALL_MS/MAX_RASTER_STDOUT_BYTES/RASTER_MAX_DIM/MIN_NATIVE_CHARS/MAX_JOB_MS)"
      exports: ["CAPS"]
    - path: "package.json"
      provides: "Pinned deps + `audit` script"
      contains: "sharp"
  key_links:
    - from: "package.json scripts.audit"
      to: "npm audit --omit=dev --audit-level=high"
      via: "npm run audit"
      pattern: "audit-level=high"
    - from: "lib/v1/input/caps.js"
      to: "lib/v1/env.js intFromEnv"
      via: "require"
      pattern: "intFromEnv"
---

<objective>
Lay the dependency + configuration foundation for the whole input pipeline: add the four pinned native/WASM decoders, ship the OPS-06 `npm audit` gate GREEN (remediating the pre-existing advisories, not aspirationally), and centralise every memory cap as a boot-validated env config.

Purpose: OPS-06 requires CVE-fixed pins scanned in CI; INP-07 requires page/DPI/pixel caps that the rasterize + streaming tasks consume. Both are prerequisites for every later plan, so they land first with zero pipeline code.
Output: Updated `package.json`/lockfile, `lib/v1/input/caps.js`, `npm run audit` green, README audit-gate note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-input-pipeline/03-CONTEXT.md
@.planning/phases/03-input-pipeline/03-RESEARCH.md
@CLAUDE.md
@lib/v1/env.js
@package.json

<interfaces>
<!-- Reuse the existing boot-validated env helpers — do NOT hand-roll Number(env)||default -->
From lib/v1/env.js:
  intFromEnv(name, fallback)   // throws on non-integer / <=0; returns fallback when unset
  floatFromEnv(name, fallback) // throws on non-finite / <=0

Pattern precedent (lib/v1/worker.js line 16, lib/v1/jobs.js line 10, lib/v1/upload.js line 5):
  const MAX_QUEUE_DEPTH = intFromEnv('MAX_QUEUE_DEPTH', 10);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pinned decoders + caps config module</name>
  <files>package.json, package-lock.json, lib/v1/input/caps.js, test/caps.test.js</files>
  <read_first>
    - CLAUDE.md §"Recommended Stack" / §"What NOT to Use" / §"Version Compatibility" (the pins + forbidden libs)
    - 03-RESEARCH.md §"Standard Stack" incl. the ⚠ NOTE that @vingle/bmp-js must pin `^0.2.5` (0.1.x does NOT exist on npm), §"Open Questions" Q1 (whole-job budget) + Q2 (concrete cap numbers)
    - lib/v1/env.js (intFromEnv/floatFromEnv contract) and the three existing call sites (worker.js:16, jobs.js:10, upload.js:5) as the analog
  </read_first>
  <action>
    Add exactly these production dependencies to package.json at the researched pins: `unpdf@^1.6.2`, `sharp@^0.35.3`, `heic-convert@^2.1.0`, `@vingle/bmp-js@^0.2.5` (NOT ^0.1.0 — that version is unpublished; the install would fail). Run the install so package-lock.json is regenerated. Do NOT add any forbidden lib (pdf2pic, pdfjs-dist@6, @napi-rs/canvas, ImageMagick/Ghostscript shims) and do NOT touch the Dockerfile — poppler-utils is already installed (Dockerfile line ~23).
    Create lib/v1/input/caps.js exporting a frozen `CAPS` object built entirely from intFromEnv (require it from ../env). Include: MAX_PDF_PAGES (default 50), RASTER_DPI (default 200), MAX_OUTPUT_PIXELS (default 25000000), RASTER_MAX_DIM (default 5000, the pdftoppm `-scale-to` long-side ceiling), ULIMIT_V_KB (default 786432 ≈ 768 MB), ULIMIT_CPU_SEC (default 20), RASTER_WALL_MS (default 30000), PDFINFO_WALL_MS (default 10000), MAX_RASTER_STDOUT_BYTES (default 41943040 ≈ 40 MB), MIN_NATIVE_CHARS (default 16), MAX_JOB_MS (default 180000 — the whole-job ceiling resolving Open Question Q1). Each key reads an env var of the same UPPERCASE name. Document each cap's units + intent in a header comment; note that exact VPS tuning is a DEFERRED Docker-smoke item.
    Write test/caps.test.js: assert defaults are present + positive integers, and that setting an env var overrides the default and an invalid value (e.g. '-5' or 'x') throws via intFromEnv (fresh require per case).
  </action>
  <verify>
    <automated>node -e "require('sharp'); require('@vingle/bmp-js'); require('heic-convert'); import('unpdf').then(()=>console.log('deps-ok'))" && node --test test/caps.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/caps.test.js` exits 0
    - `require('sharp')`, `require('@vingle/bmp-js')`, `require('heic-convert')`, and `await import('unpdf')` all succeed on the host
    - `node -e "console.log(require('./lib/v1/input/caps').CAPS.MAX_PDF_PAGES)"` prints 50
    - package.json lists all four deps at the specified carets; @vingle/bmp-js is `^0.2.5`; no forbidden lib present; Dockerfile unchanged (`git diff --quiet -- Dockerfile`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Ship the OPS-06 npm audit gate GREEN</name>
  <files>package.json, package-lock.json, README.md</files>
  <read_first>
    - 03-RESEARCH.md §"Pattern 6: CI dependency scan" + §"Open Questions" Q4 (the gate red-fails on first run against the existing advisories — must ship WITH remediation)
    - 03-CONTEXT.md D-09 AMENDED (bump offending deps and/or scope with `--audit-level=high` + documented allowlist; the gate must actually PASS at phase completion)
    - Current `npm audit --omit=dev --audit-level=high` output (form-data high, body-parser high, qs/express moderate, all `fix available via npm audit fix`)
  </read_first>
  <action>
    Remediate the pre-existing advisories so the gate passes: run `npm audit fix` (the flagged issues are transitive — form-data via multer/axios, qs+body-parser via express — and all report "fix available via npm audit fix" without an express major bump). Re-run the audit; if any high advisory remains unfixable, add a documented, time-boxed allowlist (e.g. `--audit-level=high` scoped to production deps with an explicit note of the remaining advisory ID and why) — but prefer full remediation. Do NOT jump express to v5 (CLAUDE.md forbids reworking the ported middleware this milestone).
    Add an `audit` script to package.json: `npm audit --omit=dev --audit-level=high`. Document the gate + any allowlist in README.md under a short "Dependency security (OPS-06)" heading, including how to run it and the `sharp>=0.35.0` CVE-fixed floor rationale.
    Confirm the full existing suite (`npm test`) still passes after the transitive bumps — express/multer/body-parser behavior must be unchanged (the ported route/middleware tests are the proof).
  </action>
  <verify>
    <automated>npm run audit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run audit` exits 0 (no high+ advisories in production deps)
    - The full `npm test` suite still passes (no regression from the transitive bumps) — all prior tests green
    - README.md documents the OPS-06 gate and any allowlist entries with rationale
    - `git grep -n "audit-level=high" package.json` matches the new script
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party decoder packages enter the dependency tree |
| CI → release | A future vulnerable pin must be caught before ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm installs (unpdf/sharp/heic-convert/@vingle/bmp-js) | mitigate | 03-RESEARCH §"Package Legitimacy Audit" ran slopcheck 0.6.1 + `npm view` on all four — all `[OK]`, established repos, no unexpected postinstall. Pins are exact; no `[ASSUMED]`/`[SUS]` packages, so no blocking-human legitimacy checkpoint required |
| T-03-01 | Tampering / DoS | Vulnerable native decoder pin | mitigate | `sharp>=0.35.0` CVE-fixed floor + `npm run audit --audit-level=high` gate wired into CI (OPS-06); pre-existing advisories remediated so the gate is green, not aspirational |
</threat_model>

<verification>
- `npm run audit` exits 0; `npm test` still fully green.
- caps.js drives every downstream cap from env; defaults match the research-recommended budget (Q2).
</verification>

<success_criteria>
Deps installed at CVE-fixed pins, OPS-06 audit gate GREEN and documented, and a single boot-validated caps module ready for the rasterize/streaming plans.
</success_criteria>

<output>
Create `.planning/phases/03-input-pipeline/03-01-SUMMARY.md` when done.
</output>
